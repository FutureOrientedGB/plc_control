syntax = "proto3";

package plc;


enum PlcDeviceTypeId {
    begin = 0;  // begin (not included)
    foo = 1;
    bar = 2;
    end = 3;  // end (not included)
}

message DeviceType {
    PlcDeviceTypeId id = 1; // unique type id
    string name = 2;  // unique type id string literal
}
